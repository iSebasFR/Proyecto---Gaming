@model List<Proyecto_Gaming.Models.BibliotecaUsuario>

@{
    ViewData["Title"] = "Juegos Completados";
}

<!-- Segunda Navbar Moderna - Completados (COLORES ORIGINALES) -->
<nav class="navbar navbar-expand-lg catalog-navbar">
    <div class="container-fluid justify-content-center">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" asp-action="Pendientes">Pendiente</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Jugando">Jugando</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" asp-action="Completados">Completados</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Index">Catálogo</a>
            </li>
        </ul>
    </div>
</nav>

<h2 class="page-title">Juegos Completados</h2>

@if (Model.Any())
{
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-6 mb-4">
                <div class="game-card card h-100 shadow">
                    <img src="@item.GameImage" class="card-img-top" alt="@item.GameName">
                    <div class="card-body">
                        <h5 class="card-title">@item.GameName</h5>
                        <p class="card-text">
                            <span class="badge bg-success">@item.Estado</span>
                            @if (item.FechaCompletado.HasValue)
                            {
                                <br><small class="text-muted">Completado: @item.FechaCompletado.Value.ToString("dd/MM/yyyy")</small>
                            }
                        </p>

                        <!-- Mostrar reseña existente -->
                        @if (!string.IsNullOrEmpty(item.Resena))
                        {
                            <div class="resena-section">
                                <h6>Tu Reseña:</h6>
                                <div class="star-rating">
                                    @for (int i = 1; i <= 5; i++)
                                    {
                                        <i class="fas fa-star @(i <= item.Calificacion ? "text-warning" : "text-secondary")"></i>
                                    }
                                </div>
                                <p class="resena-text">"@item.Resena"</p>
                                <small class="text-muted">Publicado: @item.FechaResena?.ToString("dd/MM/yyyy")</small>
                            </div>
                        }

                        <!-- Botón para agregar/editar reseña -->
                        <button class="btn btn-success w-100 mb-2" 
                                onclick="showReviewModal(@item.RawgGameId, '@item.GameName', @item.Calificacion, '@item.Resena')">
                            @(string.IsNullOrEmpty(item.Resena) ? "✍️ Agregar Reseña" : "📝 Editar Reseña")
                        </button>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Detalles" asp-route-id="@item.RawgGameId" class="btn btn-primary w-48">👁️ Ver Detalles</a>
                            <a asp-action="Jugando" class="btn btn-secondary w-48">🔄 Volver a Jugando</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="alert alert-success">
        <h4>No tienes juegos completados</h4>
        <p>¡Completa algunos de tus juegos en progreso!</p>
        <a asp-action="Jugando" class="btn btn-success">🎮 Ver Juegos en Progreso</a>
    </div>
}

<!-- Modal para reseñas -->
<div class="modal fade" id="reviewModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Reseña de <span id="gameName"></span></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label class="form-label">Calificación:</label>
                    <div class="star-rating" id="starRating">
                        @for (int i = 1; i <= 5; i++)
                        {
                            <i class="fas fa-star text-secondary" data-rating="@i"></i>
                        }
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label">Reseña:</label>
                    <textarea class="form-control" id="resenaText" rows="4" placeholder="Comparte tu experiencia con este juego..."></textarea>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="button" class="btn btn-success" onclick="submitReview()">📤 Publicar Reseña</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        let currentGameId = 0;
        let currentRating = 0;

        function showReviewModal(gameId, gameName, currentCalificacion, currentResena) {
            currentGameId = gameId;
            currentRating = currentCalificacion;
            
            $('#gameName').text(gameName);
            $('#resenaText').val(currentResena || '');
            
            // Resetear estrellas
            $('#starRating .fa-star').removeClass('text-warning').addClass('text-secondary');
            
            // Marcar estrellas según calificación actual
            for (let i = 1; i <= currentCalificacion; i++) {
                $(`#starRating .fa-star[data-rating="${i}"]`).removeClass('text-secondary').addClass('text-warning');
            }
            
            $('#reviewModal').modal('show');
        }

        // Manejar clic en estrellas
        $('#starRating .fa-star').on('click', function() {
            const rating = $(this).data('rating');
            currentRating = rating;
            
            // Actualizar visualización de estrellas
            $('#starRating .fa-star').each(function() {
                if ($(this).data('rating') <= rating) {
                    $(this).removeClass('text-secondary').addClass('text-warning');
                } else {
                    $(this).removeClass('text-warning').addClass('text-secondary');
                }
            });
        });

        function submitReview() {
            const resena = $('#resenaText').val();
            
            if (currentRating === 0) {
                alert('Por favor selecciona una calificación');
                return;
            }

            $.post('@Url.Action("AddReview", "Biblioteca")', {
                id: currentGameId,
                resena: resena,
                calificacion: currentRating
            }, function(response) {
                if (response.success) {
                    $('#reviewModal').modal('hide');
                    location.reload(); // Recargar para mostrar la nueva reseña
                } else {
                    alert(response.message);
                }
            });
        }
    </script>
}

@section Styles {
    <style>
        /* PALETA DE COLORES GAMING - VERSIÓN MIXTA */
        :root {
            --bg-dark: #070714;
            --bg-primary: #0f0f1f;
            --card-bg: #151530;
            --accent-purple: #8b2ce6;
            --accent-blue: #2c5be6;
            --neon-purple: #b042ff;
            --neon-blue: #428bff;
            /* COLORES VERDES PARA COMPLETADOS */
            --accent-green: #06d6a0;
            --accent-teal: #118ab2;
            --neon-green: #4ade80;
            --neon-teal: #06b6d4;
            --text-primary: #ffffff;
            --text-secondary: #b8b8d0;
            --navbar-bg: rgba(15, 15, 31, 0.95);
        }

        /* SEGUNDA NAVBAR MODERNA - COMPLETADOS (COLORES ORIGINALES) */
        .catalog-navbar {
            background: linear-gradient(135deg, var(--accent-purple), var(--neon-purple)) !important;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(139, 44, 230, 0.3);
            height: 60px;
            padding: 8px 0;
            max-width: 700px;
            margin: 0 auto 2rem auto;
            border: 1px solid rgba(139, 44, 230, 0.4);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .catalog-navbar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple), var(--accent-blue));
            background-size: 200% 100%;
        }

        .catalog-navbar .navbar-nav {
            gap: 10px;
        }

        .catalog-navbar .nav-link {
            color: var(--text-primary) !important;
            font-weight: 700;
            padding: 10px 20px !important;
            border-radius: 25px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
        }

        .catalog-navbar .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .catalog-navbar .nav-link:hover::before {
            left: 100%;
        }

        .catalog-navbar .nav-link:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.1);
        }

        .catalog-navbar .nav-link.active {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid var(--neon-blue);
            box-shadow: 0 0 20px rgba(66, 139, 255, 0.4);
            font-weight: 800;
        }

        /* TÍTULO DE PÁGINA - VERDE */
        .page-title {
            background: linear-gradient(135deg, var(--neon-green), var(--neon-teal));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 2rem;
            text-align: center;
            position: relative;
        }

        .page-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, var(--neon-green), var(--neon-teal));
            border-radius: 2px;
        }

        /* CARDS DE JUEGOS COMPLETADOS - VERDE */
        .game-card {
            background: var(--card-bg);
            border: 1px solid rgba(6, 214, 160, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            overflow: hidden;
            transition: all 0.3s ease;
            color: var(--text-primary);
            position: relative;
        }

        .game-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-green), var(--accent-teal));
            transform: scaleX(1);
            transform-origin: left;
        }

        .game-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(6, 214, 160, 0.2);
            border-color: rgba(6, 214, 160, 0.6);
        }

        .game-card .card-img-top {
            height: 250px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .game-card:hover .card-img-top {
            transform: scale(1.05);
        }

        .game-card .card-title {
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        /* BADGE DE ESTADO MEJORADO - VERDE */
        .badge.bg-success {
            background: linear-gradient(135deg, var(--accent-green), var(--accent-teal)) !important;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.8rem;
            box-shadow: 0 4px 10px rgba(6, 214, 160, 0.3);
        }

        /* SECCIÓN DE RESEÑAS - VERDE */
        .resena-section {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 1rem;
            border-left: 4px solid var(--neon-green);
        }

        .resena-section h6 {
            color: var(--neon-green);
            font-weight: 600;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
        }

        .resena-text {
            font-style: italic;
            background: rgba(255, 255, 255, 0.08);
            padding: 10px;
            border-radius: 8px;
            border-left: 3px solid var(--neon-teal);
            color: var(--text-primary);
            line-height: 1.5;
        }

        /* SISTEMA DE ESTRELLAS */
        .star-rating {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }

        .star-rating .fa-star {
            text-shadow: 0 0 8px rgba(255, 193, 7, 0.5);
            margin-right: 2px;
        }

        .star-rating .text-warning {
            color: #ffd166 !important;
        }

        /* BOTONES EN CARDS - VERDE */
        .w-48 {
            width: 48% !important;
        }

        .btn-success {
            background: linear-gradient(135deg, var(--accent-green), var(--neon-green));
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            color: white;
        }

        .btn-success::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-success:hover::before {
            left: 100%;
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(6, 214, 160, 0.4);
            color: white;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary:hover {
            box-shadow: 0 8px 20px rgba(139, 44, 230, 0.4);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #6c757d, #495057);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(108, 117, 125, 0.4);
        }

        /* MODAL MEJORADO - VERDE */
        .modal-content {
            background: var(--card-bg);
            border: 1px solid rgba(6, 214, 160, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            color: var(--text-primary);
        }

        .modal-header {
            border-bottom: 1px solid rgba(6, 214, 160, 0.3);
            background: rgba(6, 214, 160, 0.1);
        }

        .modal-title {
            color: var(--neon-green);
            font-weight: 700;
        }

        .modal-footer {
            border-top: 1px solid rgba(6, 214, 160, 0.3);
            background: rgba(6, 214, 160, 0.05);
        }

        .btn-close {
            filter: invert(1);
        }

        .form-control {
            background: rgba(255, 255, 255, 0.08);
            border: 2px solid rgba(6, 214, 160, 0.3);
            border-radius: 8px;
            color: var(--text-primary);
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.12);
            border-color: var(--neon-green);
            box-shadow: 0 0 0 0.25rem rgba(6, 214, 160, 0.25);
            color: var(--text-primary);
        }

        .form-label {
            color: var(--text-primary);
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        /* ESTRELLAS EN MODAL */
        #starRating .fa-star {
            cursor: pointer;
            font-size: 1.5rem;
            margin-right: 5px;
            transition: all 0.3s ease;
            text-shadow: 0 0 8px rgba(255, 193, 7, 0.3);
        }

        #starRating .fa-star:hover {
            transform: scale(1.2);
        }

        /* ALERTA SIN JUEGOS - VERDE */
        .alert-success {
            background: rgba(6, 214, 160, 0.1);
            border: 1px solid rgba(6, 214, 160, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            color: var(--text-primary);
            text-align: center;
            padding: 2rem;
        }

        .alert-success h4 {
            color: var(--neon-green);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .alert-success p {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
        }

        .alert-success .btn-success {
            background: linear-gradient(135deg, var(--accent-green), var(--neon-green));
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 10px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .alert-success .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(6, 214, 160, 0.4);
        }
    </style>
}