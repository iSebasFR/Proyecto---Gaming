@model Proyecto_Gaming.ViewModels.GameCatalogViewModel

@{
    ViewData["Title"] = "Catálogo de Juegos";
}

<!-- Segunda Navbar Moderna - Catálogo -->
<nav class="navbar navbar-expand-lg catalog-navbar">
    <div class="container-fluid justify-content-center">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" asp-action="Pendientes">Pendiente</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Jugando">Jugando</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Completados">Completados</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" asp-action="Index">Catálogo</a>
            </li>
        </ul>
    </div>
</nav>

<!-- ✅ BÚSQUEDA POR NOMBRE Y BOTONES DE SESIÓN MEJORADOS -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card bg-dark">
            <div class="card-body">
                <!-- Formulario de Búsqueda -->
                <form asp-action="Index" method="get" class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label class="form-label text-white">🔍 Buscar por nombre:</label>
                        <input type="text" name="search" class="form-control" 
                               value="@Model.Search" placeholder="Ej: The Witcher, Mario...">
                    </div>
                    <div class="col-md-3">
                        <label class="form-label text-white">🎮 Género:</label>
                        <select name="genre" class="form-select">
                            <option value="">Todos los géneros</option>
                            @foreach (var genre in Model.AvailableGenres)
                            {
                                <option value="@genre.Value" selected="@genre.Selected">@genre.Text</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label text-white">🖥️ Plataforma:</label>
                        <select name="platform" class="form-select">
                            <option value="">Todas las plataformas</option>
                            @foreach (var platform in Model.AvailablePlatforms)
                            {
                                <option value="@platform.Value" selected="@platform.Selected">@platform.Text</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100 h-100">
                            <i class="fas fa-search"></i> Buscar
                        </button>
                    </div>
                </form>

                <!-- ✅ BOTONES MEJORADOS -->
                <div class="row mt-3">
                    <div class="col-12">
                        <div class="d-flex gap-2 flex-wrap">
                            <!-- ✅ BÚSQUEDA ANTERIOR (SOLO cuando hay disponible) -->
                            @if (Model.HasPreviousSearch)
                            {
                                <a href="@Url.Action("LastSearch")" class="btn btn-outline-info btn-sm">
                                    🔄 Búsqueda Anterior
                                </a>
                            }

                            <!-- Recomendaciones -->
                            <a href="@Url.Action("Recomendaciones")" class="btn btn-outline-warning btn-sm">
                                💡 Recomendaciones ML
                            </a>
                            
                            <!-- Limpiar Filtros -->
                            @if (!string.IsNullOrEmpty(Model.Search) || !string.IsNullOrEmpty(Model.SelectedGenre) || !string.IsNullOrEmpty(Model.SelectedPlatform))
                            {
                                <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
                                    🗑️ Limpiar Filtros
                                </a>
                            }

                            <!-- Búsquedas Recientes Dropdown -->
                            @if (Model.RecentSearchTerms.Any())
                            {
                                <div class="dropdown">
                                    <button class="btn btn-outline-success btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                        📚 Historial (@Model.RecentSearchTerms.Count)
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li><h6 class="dropdown-header">Tus búsquedas recientes:</h6></li>
                                        @foreach (var recentSearch in Model.RecentSearchTerms)
                                        {
                                            <li>
                                                <a class="dropdown-item" href="@Url.Action("Index", new { search = recentSearch })">
                                                    <i class="fas fa-search me-2"></i>@recentSearch
                                                </a>
                                            </li>
                                        }
                                        <li><hr class="dropdown-divider"></li>
                                        <li>
                                            <a class="dropdown-item text-danger" href="@Url.Action("ClearSearchHistory")">
                                                <i class="fas fa-trash me-2"></i>Limpiar historial
                                            </a>
                                        </li>
                                    </ul>
                                </div>
                            }
                        </div>
                    </div>
                </div>

                <!-- ✅ INFO DE SESIÓN ACTUAL -->
                @if (!string.IsNullOrEmpty(Model.Search) || !string.IsNullOrEmpty(Model.SelectedGenre) || !string.IsNullOrEmpty(Model.SelectedPlatform))
                {
                    <div class="row mt-2">
                        <div class="col-12">
                            <div class="alert alert-info py-2 mb-0">
                                <small>
                                    <strong>Filtros activos:</strong>
                                    @if (!string.IsNullOrEmpty(Model.Search))
                                    {
                                        <span class="badge bg-primary me-1">Busqueda: "@Model.Search"</span>
                                    }
                                    @if (!string.IsNullOrEmpty(Model.SelectedGenre))
                                    {
                                        <span class="badge bg-success me-1">Género: @Model.AvailableGenres.FirstOrDefault(g => g.Value == Model.SelectedGenre)?.Text</span>
                                    }
                                    @if (!string.IsNullOrEmpty(Model.SelectedPlatform))
                                    {
                                        <span class="badge bg-warning me-1">Plataforma: @Model.AvailablePlatforms.FirstOrDefault(p => p.Value == Model.SelectedPlatform)?.Text</span>
                                    }
                                    <small class="text-muted ms-2">(Guardado en sesión)</small>
                                </small>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<!-- Juegos desde RAWG API -->
@if (Model.Games.Any())
{
    <div class="row">
        @foreach (var game in Model.Games)
        {
            <div class="col-md-4 mb-4">
                <div class="game-card card h-100 shadow">
                    <img src="@game.BackgroundImage" class="card-img-top" alt="@game.Name" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title">@game.Name</h5>
                        <p class="card-text">
                            @if (game.Released != null)
                            {
                                <small class="text-muted">Lanzamiento: @DateTime.Parse(game.Released).ToString("yyyy")</small>
                            }
                        </p>
                        <p class="card-text">
                            @foreach (var genre in game.Genres.Take(2))
                            {
                                <span class="badge bg-primary me-1">@genre.Name</span>
                            }
                            @foreach (var platform in game.Platforms.Take(2))
                            {
                                <span class="badge bg-secondary me-1">@platform.Platform?.Name</span>
                            }
                        </p>
                        <div class="game-rating mb-2">
                            @for (int i = 1; i <= 5; i++)
                            {
                                if (i <= game.Rating)
                                {
                                    <i class="fas fa-star text-warning"></i>
                                }
                                else if (i - 0.5 <= game.Rating)
                                {
                                    <i class="fas fa-star-half-alt text-warning"></i>
                                }
                                else
                                {
                                    <i class="far fa-star text-warning"></i>
                                }
                            }
                            <span class="ms-1">@game.Rating.ToString("0.0")</span>
                        </div>

                        <!-- ✅ BOTONES MEJORADOS - PRECIO DENTRO DEL BOTÓN -->
                        <div class="d-flex justify-content-between gap-2">
                            <a asp-action="Detalles" asp-route-id="@game.Id" class="btn btn-outline-primary btn-sm flex-fill">
                                <i class="fas fa-info-circle me-1"></i>Detalles
                            </a>
                            
                            @if (Model.GamePrices.ContainsKey(game.Id) && Model.GamePrices[game.Id].HasValue)
                            {
                                <form asp-controller="Payment" asp-action="Checkout" method="post" class="flex-fill">
                                    <input type="hidden" name="gameId" value="@game.Id" />
                                    <input type="hidden" name="gameTitle" value="@game.Name" />
                                    <input type="hidden" name="price" value="@Model.GamePrices[game.Id]" />
                                    
                                    <button type="submit" class="btn btn-success btn-sm w-100">
                                        <i class="fas fa-shopping-cart me-1"></i>Comprar - $@Model.GamePrices[game.Id]?.ToString("0.00")
                                    </button>
                                </form>
                            }
                            else
                            {
                                <a asp-action="Detalles" asp-route-id="@game.Id" class="btn btn-secondary btn-sm flex-fill">
                                    <i class="fas fa-dollar-sign me-1"></i>Ver Precio
                                </a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- Paginación - SOLO 5 PÁGINAS MÁXIMO -->
    @if (Model.TotalPages > 1)
    {
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <!-- Botón Anterior -->
                @if (Model.CurrentPage > 1)
                {
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Index", new { 
                            search = Model.Search,
                            genre = Model.SelectedGenre, 
                            platform = Model.SelectedPlatform, 
                            page = Model.CurrentPage - 1 
                        })">Anterior</a>
                    </li>
                }
                
                <!-- SOLO 5 páginas máximo -->
                @for (int i = 1; i <= Math.Min(5, Model.TotalPages); i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" href="@Url.Action("Index", new { 
                            search = Model.Search,
                            genre = Model.SelectedGenre, 
                            platform = Model.SelectedPlatform, 
                            page = i 
                        })">@i</a>
                    </li>
                }

                <!-- Botón Siguiente -->
                @if (Model.CurrentPage < 5 && Model.CurrentPage < Model.TotalPages)
                {
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Index", new { 
                            search = Model.Search,
                            genre = Model.SelectedGenre, 
                            platform = Model.SelectedPlatform, 
                            page = Model.CurrentPage + 1 
                        })">Siguiente</a>
                    </li>
                }
            </ul>
        </nav>
    }
}
else
{
    <div class="alert alert-info text-center">
        <h4>No se encontraron juegos</h4>
        <p>Intenta con otros criterios de búsqueda</p>
        @if (!string.IsNullOrEmpty(Model.Search))
        {
            <p>Buscaste: <strong>"@Model.Search"</strong></p>
        }
        <a asp-action="Index" class="btn btn-primary">Ver todos los juegos</a>
    </div>
}

@section Styles {
    <style>
        /* SEGUNDA NAVBAR MODERNA - CATÁLOGO */
        .catalog-navbar {
            background: linear-gradient(135deg, var(--accent-purple), var(--neon-purple)) !important;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(139, 44, 230, 0.3);
            height: 60px;
            padding: 8px 0;
            max-width: 700px;
            margin: 0 auto 2rem auto;
            border: 1px solid rgba(139, 44, 230, 0.4);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .catalog-navbar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple), var(--accent-blue));
            background-size: 200% 100%;
        }

        .catalog-navbar .navbar-nav {
            gap: 10px;
        }

        .catalog-navbar .nav-link {
            color: var(--text-primary) !important;
            font-weight: 700;
            padding: 10px 20px !important;
            border-radius: 25px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
        }

        .catalog-navbar .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .catalog-navbar .nav-link:hover::before {
            left: 100%;
        }

        .catalog-navbar .nav-link:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.1);
        }

        .catalog-navbar .nav-link.active {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid var(--neon-blue);
            box-shadow: 0 0 20px rgba(66, 139, 255, 0.4);
            font-weight: 800;
        }

        /* CARD DE BÚSQUEDA MEJORADA */
        .search-card {
            background: var(--card-bg);
            border: 1px solid rgba(139, 44, 230, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            position: relative;
        }

        .search-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-purple), var(--accent-blue));
        }

        .search-card .form-label {
            color: var(--text-primary);
            font-weight: 600;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .search-card .form-control,
        .search-card .form-select {
            background: rgba(255, 255, 255, 0.08);
            border: 2px solid rgba(139, 44, 230, 0.3);
            border-radius: 10px;
            color: var(--text-primary);
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
            backdrop-filter: blur(5px);
        }

        .search-card .form-control:focus,
        .search-card .form-select:focus {
            background: rgba(255, 255, 255, 0.12);
            border-color: var(--neon-purple);
            box-shadow: 0 0 0 0.25rem rgba(139, 44, 230, 0.25);
            color: var(--text-primary);
        }

        /* BOTONES DE ACCIÓN MEJORADOS */
        .action-buttons {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.85rem;
            text-decoration: none;
            transition: all 0.3s ease;
            border: 2px solid transparent;
            text-align: center;
            flex: 1;
            min-width: 120px;
        }

        .btn-outline-info {
            border-color: var(--neon-blue);
            color: var(--neon-blue);
            background: transparent;
        }

        .btn-outline-info:hover {
            background: var(--neon-blue);
            color: var(--bg-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(66, 139, 255, 0.3);
        }

        .btn-outline-warning {
            border-color: #ffd166;
            color: #ffd166;
            background: transparent;
        }

        .btn-outline-warning:hover {
            background: #ffd166;
            color: var(--bg-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 209, 102, 0.3);
        }

        .btn-outline-secondary {
            border-color: var(--text-secondary);
            color: var(--text-secondary);
            background: transparent;
        }

        .btn-outline-secondary:hover {
            background: var(--text-secondary);
            color: var(--bg-dark);
            transform: translateY(-2px);
        }

        .btn-outline-success {
            border-color: #06d6a0;
            color: #06d6a0;
            background: transparent;
        }

        .btn-outline-success:hover {
            background: #06d6a0;
            color: var(--bg-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(6, 214, 160, 0.3);
        }

        /* DROPDOWN MEJORADO */
        .dropdown-menu {
            background: var(--card-bg);
            border: 1px solid rgba(139, 44, 230, 0.3);
            border-radius: 10px;
            backdrop-filter: blur(10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .dropdown-item {
            color: var(--text-secondary);
            padding: 0.7rem 1rem;
            transition: all 0.3s ease;
            border-radius: 5px;
            margin: 0.1rem 0.5rem;
            width: auto;
        }

        .dropdown-item:hover {
            background: rgba(139, 44, 230, 0.2);
            color: var(--text-primary);
        }

        .dropdown-header {
            color: var(--neon-purple);
            font-weight: 600;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* ALERTA DE FILTROS ACTIVOS */
        .filters-alert {
            background: rgba(66, 139, 255, 0.1);
            border: 1px solid rgba(66, 139, 255, 0.3);
            border-radius: 10px;
            backdrop-filter: blur(5px);
        }

        .filters-alert .badge {
            font-size: 0.75rem;
            padding: 0.4rem 0.7rem;
            border-radius: 6px;
            font-weight: 600;
        }

        /* CARDS DE JUEGOS MEJORADAS */
        .game-card {
            background: var(--card-bg);
            border: 1px solid rgba(139, 44, 230, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            overflow: hidden;
            transition: all 0.3s ease;
            color: var(--text-primary);
            position: relative;
        }

        .game-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-purple), var(--accent-blue));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.3s ease;
        }

        .game-card:hover::before {
            transform: scaleX(1);
        }

        .game-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(139, 44, 230, 0.2);
            border-color: rgba(139, 44, 230, 0.6);
        }

        .game-card .card-img-top {
            height: 200px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .game-card:hover .card-img-top {
            transform: scale(1.05);
        }

        .game-card .card-title {
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        .game-rating {
            font-size: 1rem;
            margin-bottom: 1rem;
        }

        .game-rating .fas.fa-star,
        .game-rating .fas.fa-star-half-alt {
            text-shadow: 0 0 8px rgba(255, 193, 7, 0.5);
        }

        /* BOTONES EN CARDS - MEJORADOS */
        .btn-outline-primary {
            border-color: var(--neon-blue);
            color: var(--neon-blue);
            background: transparent;
            font-weight: 600;
            font-size: 0.85rem;
            padding: 0.5rem 0.75rem;
            border-radius: 6px;
            transition: all 0.3s ease;
        }

        .btn-outline-primary:hover {
            background: var(--neon-blue);
            color: var(--bg-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(66, 139, 255, 0.3);
        }

        .btn-success {
            background: linear-gradient(135deg, #06d6a0, #118ab2);
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.85rem;
            padding: 0.5rem 0.75rem;
            transition: all 0.3s ease;
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(6, 214, 160, 0.3);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #6c757d, #495057);
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.85rem;
            padding: 0.5rem 0.75rem;
            transition: all 0.3s ease;
            color: white;
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(108, 117, 125, 0.3);
            color: white;
        }

        .flex-fill {
            flex: 1 1 auto;
        }

        /* BADGES MEJORADOS */
        .badge {
            font-size: 0.7em;
            padding: 0.4em 0.7em;
            border-radius: 6px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .badge.bg-primary {
            background: linear-gradient(135deg, var(--accent-purple), var(--neon-purple)) !important;
        }

        .badge.bg-secondary {
            background: linear-gradient(135deg, var(--accent-blue), var(--neon-blue)) !important;
        }

        /* PAGINACIÓN MEJORADA */
        .pagination .page-link {
            background: var(--card-bg);
            border: 1px solid rgba(139, 44, 230, 0.3);
            color: var(--text-secondary);
            padding: 0.5rem 1rem;
            margin: 0 0.2rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .pagination .page-link:hover {
            background: rgba(139, 44, 230, 0.2);
            border-color: var(--accent-purple);
            color: var(--text-primary);
            transform: translateY(-2px);
        }

        .pagination .page-item.active .page-link {
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            border-color: var(--accent-purple);
            color: white;
            box-shadow: 0 5px 15px rgba(139, 44, 230, 0.3);
        }

        /* ALERTA SIN RESULTADOS */
        .alert-info {
            background: rgba(66, 139, 255, 0.1);
            border: 1px solid rgba(66, 139, 255, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            color: var(--text-primary);
        }
    </style>
}