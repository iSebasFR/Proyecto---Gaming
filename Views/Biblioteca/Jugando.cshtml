@model List<Proyecto_Gaming.Models.BibliotecaUsuario>

@{
    ViewData["Title"] = "Juegos en Progreso";
}

<!-- Segunda Navbar Moderna - Jugando -->
<nav class="navbar navbar-expand-lg catalog-navbar">
    <div class="container-fluid justify-content-center">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" asp-action="Pendientes">Pendiente</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" asp-action="Jugando">Jugando</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Completados">Completados</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Index">Catálogo</a>
            </li>
        </ul>
    </div>
</nav>

<h2 class="page-title">Juegos en Progreso</h2>

@if (Model.Any())
{
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="game-card card h-100 shadow">
                    <img src="@item.GameImage" class="card-img-top" alt="@item.GameName">
                    <div class="card-body">
                        <h5 class="card-title">@item.GameName</h5>
                        <p class="card-text">
                            <span class="badge bg-warning">@item.Estado</span>
                        </p>
                        
                        <div class="button-group">
                            <a asp-action="MarkAsCompleted" asp-route-id="@item.RawgGameId" class="btn btn-success">Marcar como Completado</a>
                            <div class="d-flex gap-2">
                                <a asp-action="Detalles" asp-route-id="@item.RawgGameId" class="btn btn-primary w-48">Ver Detalles</a>
                                <a asp-action="Pendientes" class="btn btn-secondary w-48">Volver a Pendientes</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="alert alert-info">
        <h4>No tienes juegos en progreso</h4>
        <p>¡Comienza a jugar algunos de tus juegos pendientes!</p>
        <a asp-action="Pendientes" class="btn btn-primary">Ver Pendientes</a>
    </div>
}

@section Styles {
    <style>
        /* PALETA DE COLORES GAMING */
        :root {
            --bg-dark: #070714;
            --bg-primary: #0f0f1f;
            --card-bg: #151530;
            --accent-purple: #8b2ce6;
            --accent-blue: #2c5be6;
            --neon-purple: #b042ff;
            --neon-blue: #428bff;
            --text-primary: #ffffff;
            --text-secondary: #b8b8d0;
            --navbar-bg: rgba(15, 15, 31, 0.95);
        }

        /* SEGUNDA NAVBAR MODERNA - JUGANDO */
        .catalog-navbar {
            background: linear-gradient(135deg, var(--accent-purple), var(--neon-purple)) !important;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(139, 44, 230, 0.3);
            height: 60px;
            padding: 8px 0;
            max-width: 700px;
            margin: 0 auto 2rem auto;
            border: 1px solid rgba(139, 44, 230, 0.4);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .catalog-navbar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple), var(--accent-blue));
            background-size: 200% 100%;
        }

        .catalog-navbar .navbar-nav {
            gap: 10px;
        }

        .catalog-navbar .nav-link {
            color: var(--text-primary) !important;
            font-weight: 700;
            padding: 10px 20px !important;
            border-radius: 25px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
        }

        .catalog-navbar .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .catalog-navbar .nav-link:hover::before {
            left: 100%;
        }

        .catalog-navbar .nav-link:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.1);
        }

        .catalog-navbar .nav-link.active {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid var(--neon-blue);
            box-shadow: 0 0 20px rgba(66, 139, 255, 0.4);
            font-weight: 800;
        }

        /* TÍTULO DE PÁGINA */
        .page-title {
            background: linear-gradient(135deg, #ffd166, #ff9e00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 2rem;
            text-align: center;
            position: relative;
        }

        .page-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 3px;
            background: linear-gradient(90deg, #ffd166, #ff9e00);
            border-radius: 2px;
        }

        /* CARDS DE JUEGOS EN PROGRESO */
        .game-card {
            background: var(--card-bg);
            border: 1px solid rgba(139, 44, 230, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            overflow: hidden;
            transition: all 0.3s ease;
            color: var(--text-primary);
            position: relative;
        }

        .game-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #ffd166, #ff9e00);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.3s ease;
        }

        .game-card:hover::before {
            transform: scaleX(1);
        }

        .game-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(255, 209, 102, 0.2);
            border-color: rgba(255, 209, 102, 0.6);
        }

        .game-card .card-img-top {
            height: 200px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .game-card:hover .card-img-top {
            transform: scale(1.05);
        }

        .game-card .card-title {
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        /* BADGE DE ESTADO MEJORADO - JUGANDO */
        .badge.bg-warning {
            background: linear-gradient(135deg, #ffd166, #ff9e00) !important;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.8rem;
            box-shadow: 0 4px 10px rgba(255, 209, 102, 0.3);
            color: var(--bg-dark) !important;
        }

        /* BOTONES EN CARDS */
        .w-48 {
            width: 48% !important;
        }

        .w-100 {
            width: 100% !important;
        }

        .btn-success {
            background: linear-gradient(135deg, #06d6a0, #118ab2);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            margin-bottom: 0.5rem;
        }

        .btn-success::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-success:hover::before {
            left: 100%;
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(6, 214, 160, 0.4);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary:hover {
            box-shadow: 0 8px 20px rgba(139, 44, 230, 0.4);
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: linear-gradient(135deg, var(--text-secondary), #6c757d);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 0.75rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-secondary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn-secondary:hover::before {
            left: 100%;
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(108, 117, 125, 0.4);
        }

        /* ALERTA SIN JUEGOS */
        .alert-info {
            background: rgba(255, 209, 102, 0.1);
            border: 1px solid rgba(255, 209, 102, 0.3);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            color: var(--text-primary);
            text-align: center;
            padding: 2rem;
        }

        .alert-info h4 {
            background: linear-gradient(135deg, #ffd166, #ff9e00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .alert-info p {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
        }

        .alert-info .btn-primary {
            background: linear-gradient(135deg, var(--accent-purple), var(--accent-blue));
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 10px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .alert-info .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(139, 44, 230, 0.4);
        }

        /* BOTONES FLEXIBLES */
        .button-group {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .button-group .btn {
            flex: 1;
            min-width: 120px;
        }
        
    </style>
}